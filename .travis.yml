language: java
jdk:
  - oraclejdk8
sudo:true
services:
  - mysql
dist: trusty
sudo: required
addons:
  apt:
    packages:
    - mysql-server-5.6
    - mysql-client-core-5.6
    - mysql-client-5.6

script: "mvn install"
